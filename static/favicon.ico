@bp.route('/favicon.ico')
def favicon():
    return send_from_directory(
        os.path.join(app.root_path, 'static', 'img'),  # or just 'static'
        'favicon.ico',
        mimetype='image/vnd.microsoft.icon'
    )
